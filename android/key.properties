# Debug signing for CI release builds — the keystore is committed on purpose
# (standard Android debug key, password "android"). Replace with a private
# key + these same fields for store distribution.
storePassword=android
keyPassword=android
keyAlias=androiddebugkey
storeFile=app/debug.keystore
